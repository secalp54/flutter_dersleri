import 'package:flutter/material.dart';

class AyarlaSayfasi extends StatelessWidget {
  const AyarlaSayfasi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("ayarlar"),);
  }
}